DIST_FOLDER="dist"

rm -rf "$DIST_FOLDER"
mkdir "$DIST_FOLDER"
cp source/app.css "$DIST_FOLDER"
cp source/app.js "$DIST_FOLDER"
cp source/index.html "$DIST_FOLDER"
cp source/favicon.ico "$DIST_FOLDER"
